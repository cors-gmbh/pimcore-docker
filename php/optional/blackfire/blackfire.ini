; loaded only when BLACKFIRE_ENABLED=1, see docker-entrypoint.
; Override the agent socket via the BLACKFIRE_AGENT_SOCKET environment variable.
extension=blackfire.so
blackfire.agent_socket=tcp://127.0.0.1:8307
blackfire.agent_timeout=0.25
